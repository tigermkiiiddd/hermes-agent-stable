name: byterover
version: 1.0.0
description: "ByteRover — 文件层级树记忆，本地 CLI 工具"
external_dependencies:
  - name: brv
    install: "curl -fsSL https://byterover.dev/install.sh | sh"
    check: "brv --version"
hooks:
  - on_pre_compress
